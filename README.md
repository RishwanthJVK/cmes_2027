# EEG Mind-Wandering Analysis

## Overview

This repository documents two parallel EEG analysis workflows for mind-wandering research. The original source datasets have been removed and are not distributed here. The repository contains the project analysis notebooks, derived tables, and figures only.

* `dataset 1 analysis/` contains analyses conducted with Dataset 1, a 19-channel EEG dataset.
* `dataset 2 analysis/` contains analyses conducted with Dataset 2, a 64-channel Breath Counting EEG dataset.

Both workflows model Focus and mind-wandering (MW) states. They analyse spectral power in Delta (1–4 Hz), Theta (4–8 Hz), Alpha (8–12 Hz), Beta (13–30 Hz), and Gamma (30–45 Hz), and compare raw with ICA-cleaned EEG where relevant. This README documents procedures and file roles only. It does not interpret findings.

## Contents

Each analysis directory follows this convention:

* `*.ipynb`: Kaggle-oriented Python notebook containing the analysis procedure.
* `output/csv files/`: derived tabular artefacts.
* `output/plots/`: derived figures.

The notebooks require access to their original EEG inputs, which are no longer included. Path constants must be configured to a permitted local or mounted copy of the source data before re-execution.

## Files excluded from this repository

The following derived feature tables are not included because each exceeds GitHub's 100 MB file-size limit. They can be regenerated by running the corresponding notebook with access to the required source data.

* `dataset 2 analysis/dataset_2_extratrees_and_band_importance/output/csv files/windowed_features_raw_and_ica_cleaned.csv`
* `dataset 1 analysis/dataset_1_feature_wise_analysis/output/csv files/windowed_features_all_families.csv`
* `dataset 2 analysis/dataset_2_extratrees_and_band_importance/output/csv files/ica_cleaned_wide_features.csv`
* `dataset 2 analysis/dataset_2_extratrees_and_band_importance/output/csv files/raw_wide_features.csv`

## Dataset 1 analysis

Dataset 1 is a 19-channel EEG dataset. The original dataset files and bundled MATLAB/EEGLAB, FieldTrip, E-Prime, reference, and supporting files have intentionally been removed from this repository. They are not part of this project codebase.

| Notebook | Procedure |
| --- | --- |
| `dataset_1_band_wise_analysis/dataset_1_band_wise_analysis.ipynb` | Loads labelled ICA-cleaned epochs, calculates Welch power spectra in overlapping windows, creates band-specific log-power and epoch-summary tables, fits participant-clustered state/trend models, and creates band-trajectory figures. |
| `dataset_1_raw_vs_cleaned_SVM_Logistic/dataset_1_raw_vs_cleaned_SVM_Logistic.ipynb` | Extracts spectral, envelope, burst, and phase-locking features from matched raw and ICA-cleaned epochs. It applies epoch-grouped validation, trains SVM and logistic-regression classifiers, and prepares metric and confusion-matrix artefacts. |
| `dataset_1_extratrees_and_band_importance/dataset_1_extratrees_and_band_importance.ipynb` | Extracts matched window features, applies group-preserving ExtraTrees classification, calculates feature importance for each preprocessing branch, aggregates importance by frequency band, and creates summary artefacts. |
| `dataset_1_feature_wise_analysis/dataset_1_feature_wise_analysis.ipynb` | Computes spectral, envelope, synchrony, time-domain, Hjorth, entropy, and complexity features. It uses grouped feature selection and ExtraTrees modelling and produces feature- and family-level importance inventories. |
| `dataset1_deep_mlp/dataset1_deep_mlp.ipynb` | Forms window-level feature vectors for raw and ICA-cleaned data, performs foldwise standardisation, trains a multilayer perceptron, aggregates predictions to epochs, and creates classification artefacts. |

### Dataset 1 derived files

* `dataset_1_band_wise_analysis/output/csv files/all_band_window_level_data.csv` stores window-level band-power records. The five `*_epoch_summaries.csv` files store band-specific epoch summaries, and `all_band_statistical_results.csv` stores the model output fields. The six files in `output/plots/` visualise individual-band and combined standardised trajectories.
* `dataset_1_raw_vs_cleaned_SVM_Logistic/output/csv files/df_main_phase1.csv` is the retained label/metadata table. `all_epoch_features.csv` stores classifier inputs, and `classification_metrics.csv` stores evaluation fields. The five PNGs in `output/plots/` are SVM/logistic-regression confusion matrices and metric displays.
* `dataset_1_extratrees_and_band_importance/output/csv files/raw_and_clean_windowed_features.csv` is the matched feature matrix. `raw_feature_importance.csv` and `ica_cleaned_feature_importance.csv` store branch-specific importances, `bandwise_feature_importance.csv` aggregates them by band, and `raw_vs_clean_metrics.csv` stores evaluation fields. The four figures visualise these artefacts.
* `dataset_1_feature_wise_analysis/output/csv files/feature_importance_by_feature.csv` and `feature_importance_by_umbrella.csv` record individual-feature and feature-family importance fields. The two plots display the corresponding inventories.
* `dataset1_deep_mlp/output/csv files/raw_vs_clean_metrics.csv` stores DeepMLP evaluation fields. Its three plots provide two confusion matrices and a metric display.

## Dataset 2 analysis

Dataset 2 is a 64-channel Breath Counting EEG dataset. Its BIDS-style recordings, channel/electrode metadata, anatomical images, and associated source materials have been removed and are not included in this repository.

| Notebook | Procedure |
| --- | --- |
| `dataset_2_band_wise_analysis/dataset_2_band_wise_analysis.ipynb` | Selects EEG channels, aligns trigger events after resampling, filters recordings, performs EXG-correlated ICA cleaning, derives Focus/MW epochs, calculates Welch band power in overlapping windows, fits clustered models, and writes tables and figures. |
| `dataset_2_raw_vs_cleaned_SVM_Logistic/dataset_2_raw_vs_cleaned_SVM_Logistic.ipynb` | Creates matched raw and ICA-cleaned processing branches, extracts window features, applies grouped validation, fits SVM and logistic-regression classifiers, aggregates epoch predictions, and exports diagnostic artefacts. |
| `dataset_2_extratrees_and_band_importance/dataset_2_extratrees_and_band_importance.ipynb` | Produces raw and cleaned feature matrices, applies grouped ExtraTrees classification and band-level importance aggregation, and saves feature/importance tables and figures. |
| `dataset_2_feature_wise_analysis/dataset_2_feature_wise_analysis.ipynb` | Extracts spectral, temporal, entropy/complexity, envelope, and connectivity feature families. It applies grouped feature selection and ExtraTrees modelling and exports feature and family inventories. |
| `dataset2_deepmlp/dataset2_deepmlp.ipynb` | Builds BDF-derived window vectors for raw and ICA-cleaned branches, trains a multilayer perceptron with foldwise imputation/scaling and early stopping, and aggregates window probabilities to epochs. |

### Dataset 2 derived files

* `dataset_2_band_wise_analysis/output/csv files/all_band_window_level_data.csv` stores window-level band-power data. The five `*_epoch_summaries.csv` files store per-band epoch summaries, and `all_band_statistical_results.csv` stores model output fields. The six associated figures present individual and combined band trajectories.
* `dataset_2_raw_vs_cleaned_SVM_Logistic/output/csv files/all_epoch_features.csv` is the classifier feature table, while `classification_metrics.csv` stores model and metric fields. The five associated figures are classifier confusion matrices and a combined metric display.
* `dataset_2_extratrees_and_band_importance/output/csv files/` contains `raw_feature_importance.csv`, `ica_cleaned_feature_importance.csv`, `raw_bandwise_feature_importance.csv`, `ica_cleaned_bandwise_feature_importance.csv`, and `raw_vs_ica_cleaned_classification_metrics.csv`. These retain feature-importance and evaluation fields. The four figures visualise the same workflow.
* `dataset_2_feature_wise_analysis/output/csv files/feature_importance_by_feature.csv` and `feature_importance_by_umbrella.csv` retain individual-feature and feature-family inventories. Its two figures display these inventories.
* `dataset2_deepmlp/output/csv files/window_sample_metadata.csv` stores non-vector identifiers for the DeepMLP windows. `raw_vs_ica_cleaned_deepmlp_metrics.csv` stores metric fields. Its three figures provide confusion matrices and a metric display.
* `failed_runs.csv`, where present, is a structured placeholder for processing exceptions.

## Requirements

Re-executing the notebooks requires Python 3 and the original permitted datasets. The workflows use MNE, NumPy, pandas, SciPy, statsmodels, scikit-learn, matplotlib, seaborn, and PyTorch for the DeepMLP workflow. Kaggle-specific paths in the notebooks must be changed for local use.
